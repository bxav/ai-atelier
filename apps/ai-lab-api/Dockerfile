# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build agent-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t agent-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system ai-lab-api && \
          adduser --system -G ai-lab-api ai-lab-api

COPY dist/apps/ai-lab-api ai-lab-api

### Temporary workaround for prisma
#COPY apps/agent-api/prisma agent-api/prisma

RUN chown -R ai-lab-api:ai-lab-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix ai-lab-api --omit=dev -f install


### Temporary workaround for prisma
# COPY node_modules/@prisma agent-api/node_modules/@prisma
# RUN (cd agent-api; npx prisma generate)

CMD [ "node", "ai-lab-api" ]
